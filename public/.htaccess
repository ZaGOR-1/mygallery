# PHP upload limits are configured in php.ini, Apache VirtualHost, or PHP-FPM pool settings.
# Keeping php_value out of this file makes the project portable to PHP-FPM/CGI hosting.
ServerSignature Off

<IfModule mod_headers.c>
    Header always unset X-Powered-By
    Header always unset Server
</IfModule>
