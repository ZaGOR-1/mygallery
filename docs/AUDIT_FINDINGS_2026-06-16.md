# MyGallery — Повний аудит (2026-06-16)

Аудит проєкту **MyGallery v6.4.6** на гілці `beta`: безпека, баги, надійність, цілісність даних, реліз-гігієна. Цей файл — окремий робочий звіт зі списком того, що треба виправити. Канонічний короткий summary живе в `docs/AUDIT_REPORT.md`.

## Як проводився аудит

- Прочитано канонічні інструкції: `AGENTS.md`, `CLAUDE.md`, `docs/BUGS.md`.
- Поглиблено перевірено ядро: `auth.php`, `auth_functions.php`, `csrf.php`, `db.php`, `config.php`.
- Поглиблено перевірено підсистеми (upload/image, admin CRUD, share/download/login, public gallery, tools/release/backup).
- Запущено статичні перевірки на **PHP 8.3.14** (а не на WampServer-default PHP 7.4 — на 7.4 проєкт навмисно не парситься через union types та інші 8.x-фічі; це не баги коду).

### Результати автоматичних перевірок

| Перевірка | Результат |
|---|---|
| `php -l` по всіх `*.php` (PHP 8.3) | ✅ Без синтаксичних помилок |
| `node --check public/assets/js/main.js` | ✅ OK |
| `php tests/run.php` | ✅ 9 passed, 0 failed |
| `php tools/self_check.php` | ✅ Self-check passed |

## Загальний висновок

Проєкт **архітектурно зрілий і в основному безпечний**: послідовно застосовані PDO prepared statements, CSRF, `htmlspecialchars(ENT_QUOTES)`, рандомізовані імена файлів, сувора валідація шляхів проти traversal, whitelist для `ORDER BY`, біндинг `LIMIT/OFFSET`, транзакції з rollback, trash-based delete. Командних інʼєкцій, прямих SQL-інʼєкцій з вводу користувача чи витоку stack trace у production не знайдено.

Проте є **2 серйозні проблеми, які треба виправити до релізу** (бувпас захисту від брутфорсу і витік приватних оригіналів), а також низка проблем надійності/цілісності даних та дрібних дефектів.

> ⚠️ `docs/BUGS.md` стверджує «Критичних відомих проблем немає». Це більше не відповідає дійсності — оновити після виправлень.

---

## 🔴 CRITICAL

### C1. Захист входу від брутфорсу обходиться тривіальною арифметичною CAPTCHA — ✅ ВИПРАВЛЕНО (v6.4.7)
**Файл:** `public/admin/login.php:200-217`

> **Статус:** виправлено. CAPTCHA-механізм прибрано; локаут тепер суто час-базований (`locked_until`): поки bucket залоковано, спроба входу відхиляється з заголовком `Retry-After`, і ніщо не обнуляє лок. Перевірено: `php -l` ✅, `php tests/run.php` → 9/9 ✅.

Коли bucket залоковано (`$lockSecondsLeft > 0`), єдиний наслідок — математична CAPTCHA виду `num1 + num2`, де **обидва доданки показані користувачу** (`login_captcha_question`, рядок 212/276). При правильній відповіді код робить `$lockSecondsLeft = 0` (рядок 216) і вхід продовжується штатно. Простір відповідей — суми 2–20, а сам приклад відкрито в HTML, тож автоматизований атакер розвʼязує його кожного разу і **продовжує необмежений підбір пароля**. Часового локауту, який реально відмовляв би у спробі, немає.

Це підриває вимогу `AGENTS.md` «обмежуй невдалі спроби входу через `login_attempts`».

**Фікс:** коли `login_lock_seconds_for() > 0`, **жорстко відхиляти** спробу входу з генеричною помилкою незалежно від CAPTCHA (повертати «Забагато спроб, зачекайте N сек.»), або замінити арифметику на справжню непередбачувану CAPTCHA І зберегти часовий тротлінг. Розвʼязана CAPTCHA не повинна обнуляти часовий лок.

---

## 🟠 HIGH

### H1. Завантаження альбому ZIP віддає приватні byte-for-byte оригінали анонімним користувачам
**Файл:** `public/download_album.php:208-212` (+ token-гілка `94-122`)

У циклі формування ZIP першим джерелом є `storage/originals` (рядок 209), і лише як fallback — `uploads/large`. Тобто **публічне** завантаження альбому (`?album_id=`) або завантаження за share-токеном віддає повнорозмірні приватні оригінали. Це суперечить правилу `AGENTS.md` «download original тільки для адміна» та «приватні byte-for-byte оригінали в `storage/originals`».

Додатково: token-гілка (рядки 94-122) не перевіряє `albums.is_private` (на відміну від `album_id`-гілки на рядку 139). Для share-лінка це прийнятно (лінк свідомо надає доступ), але оригінали все одно не мають витікати.

**Фікс:** ухвалити явну політику. Якщо ZIP-завантаження для не-адмінів має містити лише оптимізовані версії — для не-адмінських/токен-завантажень брати тільки `uploads/large`, а `storage/originals` лишити виключно за `is_admin_logged_in()`. Якщо оригінали в публічному ZIP справді задумані — задокументувати це і прибрати з правил «originals only for admin».

---

## 🟡 MEDIUM

### M1. Сира інтерполяція `implode($photoIds)` у SQL (порушення політики prepared statements)
**Файл:** `public/admin/bulk_edit.php:68, 82`

Два запити `cover_photo_id = NULL` вставляють `implode(',', $photoIds)` прямо в SQL-рядок, тоді як сусідні рядки (70-72, 84-87) для того ж масиву коректно використовують `?`-плейсхолдери. **Наразі не експлуатується**, бо `$photoIds` пройшов `array_map('intval')` + `array_filter(>0)` (рядки 27-28), але це крихкий патерн: стане інʼєкцією після будь-якого рефакторингу санітизації, і прямо порушує «тільки prepared statements».
**Фікс:** використати той самий `array_fill(0, count($photoIds), '?')` і передати `$photoIds` як bound params. Також прибрати дубльований коментар `// Remove from album` (рядки 66-67).

### M2. Похідні WebP/AVIF осиротіються при збої upload, а тихі збої лишають биті файли
**Файли:** `app/includes/photo_service.php` (cleanup ~134-136), `app/includes/file_functions.php` (`create_webp_copy`/`create_avif_copy` ~680-712)

1. Catch-блок upload видаляє лише `original/large/thumbnail`, але не `.webp`/`.avif`-сіблінги — при збої після їх створення вони лишаються на диску.
2. `@imagewebp(...)`/`@imageavif(...)` ігнорують результат: частковий/нульовий запис створює биті `.webp`/`.avif`, які потім проходять перевірку «існує» і потрапляють у `srcset`.

**Фікс:** у cleanup-блоці видаляти також похідні `.webp`/`.avif`; перевіряти результат `imagewebp/imageavif` і `unlink` частковий файл при невдачі.

### M3. Відновлення БД та міграції не транзакційні — частковий збій лишає неконсистентний стан
**Файли:** `tools/restore.php:275-285`, `tools/migrate.php:33-39`

`restore.php` виконує дамп `DELETE`+`INSERT` одним `exec()` без транзакції/safety-backup; збій посередині лишає БД напіввідновленою, а медіа от-от буде стерто. `migrate.php` виконує кожен файл одним `exec()`; DDL у MySQL автокомітиться, тож при частковому збої міграція не записується і повторний запуск падає.
**Фікс:** робити safety-backup перед restore (або прибирати `LOCK TABLES` з дампу і обгортати застосування в транзакцію); вимагати/документувати ідемпотентність кожної міграції (`IF NOT EXISTS`/`IF EXISTS`).
*(Зайвий мертвий код: `restore.php:268-273,285` пише `$tmpSql`, який ніде не використовується.)*

### M4. `schema_migrations` не входить у backup → після restore міграції перезапускаються
**Файл:** `tools/backup.php:111, 174`

Список таблиць для дампу — хардкод-allowlist без `schema_migrations`. Після restore цей реєстр порожній, і `migrate.php` повторно проганяє всі міграції по вже мігрованих даних.
**Фікс:** додати `schema_migrations` до експорту (або формувати список таблиць динамічно).

### M5. `throw clone $exception` псує стек і може падати
**Файл:** `app/includes/photo_service.php:289` (`delete_album_with_validation`)

Клонування `Throwable` перед re-throw безглузде й шкідливе: губить оригінальний стек/ідентичність і ламається на неклонованих винятках. Майже напевно одрук замість `throw $exception;`.
**Фікс:** прибрати `clone`.

### M6. Приватність альбомів залежить від `global $isSharedView` — крихка звʼязність
**Файл:** `app/includes/functions.php:848-853`

`build_gallery_where_clause()` читає `global $isSharedView`, щоб вирішити, чи прибирати фільтр приватності. Сьогодні безпечно, але будь-який майбутній виклик, що виставить цей глобал перед count/fetch, мовчки відкриє приватні альбоми.
**Фікс:** передавати рішення про приватність явним аргументом функції, а не через глобал.

### M7. Cooldown завантаження ZIP привʼязаний до підробного User-Agent
**Файл:** `public/download_album.php:20-22`

Ключ кулдауна — `sha256(IP | User-Agent | scope)`. Анонімний клієнт, змінюючи UA, отримує свіжий 90-сек bucket щоразу, обходячи захист від вичерпання ресурсів (ZIP до 200 фото / 500 МБ).
**Фікс:** ключувати кулдаун лише за IP + scope (album/token), без UA; або глобальний per-IP бюджет.

### M8. `share.php` rate-limit без `flock` + можливий null-deref на битому JSON
**Файл:** `public/share.php:13-20`

Read-modify-write JSON-лічильника без блокування → втрати оновлень при конкуренції (лічильник занижується). Якщо файл містить некоректний JSON, `$limitArr` стає `null`, і звернення `$limitArr['time']` дає warning/`TypeError` під strict_types.
**Фікс:** використати `flock` (як уже зроблено в `download_album.php`) і валідувати форму масиву після `json_decode`.

---

## 🟢 LOW

| ID | Файл:рядок | Проблема | Фікс |
|---|---|---|---|
| L1 | `public/admin/health.php:100-101` | Порожній `foreach` — статус **обовʼязкових** PHP-розширень ніколи не виводиться (показуються лише WebP/AVIF). | Заповнювати `$extensionRows[]` через `extension_loaded($extension)` усередині циклу. |
| L2 | `public/admin/share.php:64, 81, 95` | Audit-лог пише **повні токени у відкритому вигляді** в `storage/logs/share_audit.log` — читач лога отримує робочі share-лінки. | Логувати хеш/префікс токена, не сам секрет. |
| L3 | `public/404.php:14` | `http_response_code(404)` захардкоджено, ігнорує обчислений `$errorStatusCode`. | `http_response_code($errorStatusCode >= 400 ? $errorStatusCode : 404);` як у `500.php`. |
| L4 | `public/admin/edit.php:~99` | На невдалому POST-рендері optimistic-lock токен `updated_at` скидається на поточне значення з БД → лок слабший, ніж здається. | Перезаповнювати hidden-поле з `$_POST['updated_at']` при POST. |
| L5 | `public/admin/download.php:48` | `Content-Type` захардкоджено `image/jpeg`, хоча віддаються й `.webp/.avif`. | Брати з `$photo['mime_type']` або розширення. |
| L6 | `app/includes/file_functions.php:~667` | `get_image_dominant_color` ігнорує результат `imagecopyresampled` → можливий сміттєвий колір замість помилки. | Перевіряти результат, повертати `null` при збої. |
| L7 | `public/admin/trash.php:62-88` | Сортування за **відформатованим** рядком дати (`d.m.Y H:i`) лексикографічно неправильне між місяцями/роками. | Сортувати за сирим ISO-`created_at`, форматувати лише для виводу. |
| L8 | `tools/verify_backup.php:84-94` | Розбіжність кількості файлів дає лише warning і `exit 0` — пошкоджений/урізаний backup проходить перевірку. | `exit` ненульовим при розбіжності; переюзати валідацію з `restore.php`. |
| L9 | `public/admin/index.php:58` | Кламп пагінації лише зверху (`min($page,$totalPages)`); нижня межа тримається на віддаленому інваріанті. | `max(1, min($page, $totalPages))` як у `trash.php`. |
| L10 | `public/gallery.php` (shared-гілка) | `$token` використовується без `?? ''` і без `h()`-екранування у URL у shared-режимі. | Додати дефолт `$token = $token ?? ''` і екранувати. |
| L11 | `tools/cleanup_runtime.php:56-77` | `getRealPath()` + `@unlink/@rmdir` без confinement → пішов би за симлінком поза базовою текою. | Перевіряти, що шлях усередині дозволеної бази, перед видаленням. |
| L12 | `app/includes/photo_service.php:~46` | `ini_set('memory_limit','512M')` безумовно — може **знизити** вищий ліміт хоста. | Підвищувати лише через `max(...)` або відновлювати після. |

---

## ℹ️ Підтверджено як коректне (без дій)

- **Upload-валідація**: JPEG примусово через `finfo_file` **і** `getimagesize`; розширення не довіряється; імʼя рандомізоване; оригінал у `storage/originals`; ліміти розміру/габаритів/пікселів перевіряються до GD.
- **GD на основному resize-шляху**: результати `imagecreatetruecolor/imagecopyresampled/imagerotate/imageflip/imagejpeg` перевіряються, є `imagedestroy`.
- **EXIF Orientation 1–8**: оброблено коректно, включно зі свопом W/H для 5/6/7/8.
- **Path traversal**: `valid_photo_filename` (`^[a-f0-9]{32}\.(jpg|webp|avif)$`) + `realpath` + `str_starts_with(base)` усюди; restore валідує ZIP-entries до мутацій; tools мають CLI-only guard.
- **SQL**: усе на PDO prepared statements; `ORDER BY` whitelisted; `LIMIT/OFFSET` біндяться `PARAM_INT`.
- **CSRF / require_admin**: усі адмін-сторінки під `require_admin()`; усі state-changing POST під `require_csrf()`; логін/логаут — POST+CSRF; `session_regenerate_id(true)` після входу; dummy-hash проти user enumeration; генерична помилка входу.
- **Приватність у public**: `photo.php`/`gallery.php`/`albums.php` коректно ховають приватні альбоми від не-адмінів (з застереженням M6).
- **Debug-витоки**: `display_errors`/stack trace під `APP_DEBUG`; production hard-fail при debug=on; SQL-помилки не показуються.
- **Реліз-гігієна**: `build_release.php` виключає `.git/.env/config/database.php`/медіа/логи/сесії/backups/dist/`temp_*`/`*.bak/*.tmp/*.zip` (подвійний захист).
- **Без command injection**: жодного `exec/shell_exec/system/proc_open/passthru`.

---

## Рекомендований порядок виправлень

1. **C1** — закрити обхід локауту входу *(блокер релізу)*.
2. **H1** — визначити політику і прибрати витік оригіналів у ZIP *(блокер релізу)*.
3. **M5** — `throw clone` (однорядковий фікс, ризик прихованих збоїв delete).
4. **M2** — осиротілі/биті WebP/AVIF.
5. **M1** — прибрати сиру SQL-інтерполяцію в `bulk_edit`.
6. **M3, M4** — транзакційність restore/міграцій + `schema_migrations` у backup *(цілісність даних)*.
7. **M6, M7, M8** — крихка приватність, UA-кулдаун, flock у share.
8. **L1–L12** — дрібні дефекти й хардненінг.
9. Оновити `docs/BUGS.md` (прибрати твердження «критичних проблем немає»), `CHANGELOG.md`, `docs/AUDIT_REPORT.md` після фіксів.

## Що НЕ перевірялося

- Динамічне тестування (живий брутфорс, реальний upload, відкриття/ревок share у браузері) — лише статичний аналіз коду.
- Поведінка на реальному MySQL/MariaDB та під конкурентним навантаженням.
- Перевірка `unzip -t` готового release ZIP (на Windows `unzip` недоступний; збірка ZIP не запускалася в цьому аудиті).
- Конфігурація Apache/Nginx, реальні security-headers у відповіді сервера.
